{
  "name": "aws-auth",
  "version": "1.0.0",
  "scripts": {
    "offline": "sls offline start --skipCacheInvalidation",
    "production": "rm -rf ./node_modules && npm i --production"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "bcryptjs-then": "^1.0.1",
    "jsonwebtoken": "^8.4.0",
    "mongoose": "^5.13.15"
  },
  "devDependencies": {
    "serverless-offline": "^3.31.3"
  }
}
